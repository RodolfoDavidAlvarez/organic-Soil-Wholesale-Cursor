// Backward-compatible alias for legacy imports. Authentication is never bypassed.
export { adminAuthMiddleware as tempAdminAuthMiddleware } from "./adminAuth";
export type { AdminRequest } from "./adminAuth";
